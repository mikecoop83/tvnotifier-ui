generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "cockroachdb"
  url      = env("DATABASE_URL")
}

model Shows {
  id   Int    @id(map: "pk")
  name String @db.String(256)

  @@map("shows")
}
